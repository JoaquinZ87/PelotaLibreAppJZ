<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema del sistema (Activity/splash). El look real de la UI lo pone Compose.
         DeviceDefault.NoActionBar no requiere librerías extra. -->
    <style name="Theme.PelotaLibreTV" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>
</resources>
